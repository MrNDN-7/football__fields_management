<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Trạng thái bình thường -->
    <item android:drawable="@drawable/home" android:state_enabled="true"/>
    <!-- Trạng thái được chọn -->
    <item android:drawable="@drawable/add" android:state_checked="true"/>
    <!-- Trạng thái không được bật -->
    <item android:drawable="@drawable/heart" android:state_pressed="true"/>
</selector>